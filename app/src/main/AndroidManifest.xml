<?xml version="1.0" encoding="utf-8"?>
 <manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.openclassrooms.reunion">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name="com.openclassrooms.reunion.MyApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.openclassrooms.reunion.ui.reunion_list.AddReunionActivity"
            android:label="@string/activity_new_neighbour" />
        <activity
            android:name=".ui.reunion_list.ViewNeighbourActivity"
             />
        <activity
            android:name="com.openclassrooms.reunion.ui.reunion_list.ListReunionActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>